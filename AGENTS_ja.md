# 開発ガイド

## 基本方針

- 変更は目的を絞り、既存の設計と命名規則に従う。
- 関連するドキュメントを、実装変更と同じ変更に含める。
- 不明な仕様や影響範囲が大きい変更は、着手前に確認する。
- 想定した完了地点まで進めず停止する場合は、停止理由・完了済み範囲・次に必要な対応を明確に知らせる。
- やりとりは全て日本語でお願いします
- やりとりは日本語のまま、アプリ内の表示文言・コメント・識別子は英語で作成してください
- アプリ内の表示文言はすべて英語にしてください
- ただし、SpecKitで生成するドキュメントについては日本語にしてください
  - 特に tasks.md の作成時に英語のままになりやすいので注意してください
- 提出するドキュメントは英語版を正本とする。日本語版を併記する場合は `*_ja.md`、英語版は対応する `*.md` のファイル名とする。
- SpecKitのagent-context更新が発生した場合は、`<!-- SPECKIT START -->` と `<!-- SPECKIT END -->` の間を、対象SPECの最新 `plan.md` を参照する内容へ更新する。自動更新が実行できない場合も手動で更新する。
- エスカレーションが必要な場合は迂回案や代替案を検討せずエスカレーションしてください
- 原則として、外部サービス・外部LLM・ローカルLLM・OS標準のAI／翻訳機能を含め、ChatGPTまたはCodex以外のサービスやモデルを使用しない。
- 他のサービスやモデルの利用がどうしても必要な場合は、利用対象・必要な理由・処理されるデータを説明し、使用前にユーザーの明示的な承認を得る。
- Python を使わなくて済む処理はできるだけ Python を使わないでください。どうしても Python 系のソフトウェアをインストールする必要がある場合はシステムに直接インストールせず、 venv をつかって仮想環境を利用してください

## Spec Implement の進め方

- 該当の Spec の tasks.md にある未完了項目をすべて完了させてください
- ただし、確認事項、問題があれば開発・テストを停止してください
- 途中報告をすると作業が停止してしまうので、完了するまで報告をしないでください。最終回答をしそうになったらこの依頼内容を確認してください
- 手動テストは開発できる箇所が全て完了後にまとめて実施します

## 仕様ドキュメント

開発の際は以下を参照の上、進めてください

- IDEA.md : 企画が記述されたファイル

## 品質

- 変更した振る舞いは、適切なテストで確認する。
- テストが実行できない場合は、理由と未確認事項を共有する。
- フォーマッターやリンターが設定されている場合は実行する。

## Git

- 他者の変更を上書き・削除しない。
- コミットは小さくまとめ、変更理由が分かるメッセージにする。
- Codexの制限付き実行環境で`gh`が認証、keyring、または`api.github.com`への接続エラーを返した場合、ユーザーへ再認証を依頼する前に、同じ確認を権限昇格した環境で1回再試行する。
  - 書き込みを伴う`gh`コマンドが失敗した場合は、PRなどの対象が既に作成・更新されていないか読み取りコマンドで確認してから再試行し、重複操作を防ぐ。
  - 権限昇格した環境でも`gh auth status`が失敗した場合に限り、ユーザーへ`gh auth login`を依頼する。
- `gh pr create` / `gh pr edit` で本文を設定する際は、改行崩れ防止のため `--body` を使わず、必ず `--body-file` を使う
- 手順:
  1. 一時Markdownファイル（例: `/tmp/pr_body.md`）に本文を書く
  2. `gh pr create --body-file /tmp/pr_body.md`（または `gh pr edit --body-file /tmp/pr_body.md`）を実行する
  3. `gh pr view --json body --jq .body` で改行が保持されていることを確認する

## テスト方針

- 原則としてユニットテストが可能な関数はテストの作成をしてください。特に、contract の固定、分岐の多い判定、純粋ロジックの境界条件はテストをお願いします
- ユーティリティ関数のテストは、再利用される純粋関数、または条件分岐が多く integration test だけでは切り分けづらい関数に限って追加してください
- 薄いラッパー、単純な委譲、外部ライブラリ呼び出しの接着だけの関数には原則として個別のユニットテストを追加しないでください (ただし、何らかの処理を含んでいる場合は追加)
- repository / store / schema など source of truth に近い層では unit test を優先し、MCP 接続や複数層をまたぐ導線は integration test で保証してください
- UI は状態分岐や重要な表示切り替えを unit test で押さえ、最終的な導線確認は quickstart や manual test で補完してください

## Codex利用記録

- 企画・設計・実装・調査・不具合解決・重要な判断をCodexで行った場合、作業完了前に `USE_CODEX.md` を更新する。
- 日付ごとに見出しを作成し、使用した厳密なモデル名と内容を箇条書きで記録する。
- 単純な確認・雑談・内容を変更しない質問への回答は記録しない。
